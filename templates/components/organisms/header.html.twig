<header>
	<nav>
		<ul class="menu">
			{% if app.user %}
			<li class="menu-link home-link">
				<a href="{{ path('app_front_homepage') }}">Home</a>
			</li>
			<li class="menu-link quiz-link">
				<a href="{{ path('app_front_quizz') }}">Quiz</a>
			</li>
			{% endif %}
			<li class="menu-link profile-link">
				<span>Profile</span>
				<ul class="sub-menu">
					{% if app.user %}
						<li>
							<a href="">My settings</a>
							<a href="">My statistics</a>
							<a href="{{ path('app_logout')}}">Log-out</a>
						</li>
					{% else %}
						<li>
							<a href="{{ path('app_security_register')}}">Register</a>
							<a href="{{ path('app_login')}}">Log-in</a>
						</li>
					{% endif %}
				</ul>
			</li>
		</ul>
	</nav>
</header>
